# -----------------------------------------------------------------------------
# Terraform – do not commit secrets, state, or local overrides
# -----------------------------------------------------------------------------

# Local variable values (often contain secrets; use terraform.tfvars.example as template)
terraform.tfvars
*.tfvars.local
*.auto.tfvars.local

# State and lock (use remote backend in production; keep state out of git)
*.tfstate
*.tfstate.*
.terraform.tfstate.lock.info

# Provider and module cache (regenerated by terraform init)
.terraform/

# Crash and debug logs
crash.log
crash.*.log

# Local overrides (avoid committing; use for one-off local changes)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# -----------------------------------------------------------------------------
# Keep in git: .terraform.lock.hcl (for reproducible provider versions)
# -----------------------------------------------------------------------------
