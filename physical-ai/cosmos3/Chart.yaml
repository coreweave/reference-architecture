apiVersion: v2
name: cosmos3-data-flywheel
description: |
  End-to-end Cosmos3 data flywheel on CoreWeave Kubernetes Service —
  synthetic data generation, SFT fine-tuning, model export, and live serving
  via Ray Serve. Every pipeline step is a values.yaml entry; enable the
  ones you want and `helm template . | kubectl apply -f -`.
type: application
version: 0.1.0
appVersion: "0.9"
keywords:
  - cosmos3
  - physical-ai
  - robotics
  - world-foundation-model
  - coreweave
  - cks
home: https://github.com/coreweave/reference-architecture/tree/main/physical-ai/cosmos3
maintainers:
  - name: CoreWeave Solutions Architecture
